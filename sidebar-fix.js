// Sidebar compatibility shim.
// The calculator core now owns sidebar navigation. This file intentionally does nothing
// so legacy deployments can keep loading it without creating a second controller.
